<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- TTS engine visibility for spoken corrections (Android 11+ package visibility). -->
    <queries>
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE" />
        </intent>
    </queries>

    <!-- Live target observation and still capture of the shot target. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera.any" android:required="false" />

    <!-- External camera feeds: RTSP/HTTP-MJPEG spotting cameras downrange
         and scope Wi-Fi streams. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- Pins the app to the camera's own access point while a stream is
         playing. Without it requestNetwork is refused, sockets go out over
         the default route — mobile data — and the camera's 192.168.x.x is
         unreachable, which is exactly the failure that was reported. -->
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />

    <!-- Acoustic shot gating: the report timestamps the frame in which a new
         hole should appear, which suppresses false positives from target
         movement and light changes. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Kestrel weather meter over BLE (integrated from VTB): bonded connect,
         plus a short advertising-only scan for the DROP D3. neverForLocation
         so no location permission is needed on Android 12+. -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />

    <!-- Reading a user-supplied target image on API 26-32. Scoped storage
         from 33 uses the photo picker, which needs no permission. -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:name="com.rfsat.bas.BasApp"
        android:largeHeap="true"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.BAS"
        android:supportsRtl="true">

        <activity
            android:name="com.rfsat.bas.ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.rfsat.bas.detect.SessionActivity" android:exported="false" />
        <activity android:name="com.rfsat.bas.detect.ImportActivity" android:exported="false" />
        <activity android:name="com.rfsat.bas.results.ResultsActivity" android:exported="false" />
        <activity android:name="com.rfsat.bas.targets.TargetActivity" android:exported="false" />
        <activity android:name="com.rfsat.bas.profiles.ProfileActivity" android:exported="false" />
        <!-- Ballistics flow (integrated from VTB) -->
        <activity android:name="com.rfsat.bas.capture.CaptureActivity" android:exported="false" />
        <activity android:name="com.rfsat.bas.results.BallisticsResultsActivity" android:exported="false" />
        <activity android:name="com.rfsat.bas.ui.RangeActivity" android:exported="false" />
        <activity android:name="com.rfsat.bas.ui.WelcomeActivity" android:exported="false" />
        <activity android:name="com.rfsat.bas.rules.RulesActivity" android:exported="false" />
        <activity android:name="com.rfsat.bas.log.LogActivity" android:exported="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>
</manifest>
